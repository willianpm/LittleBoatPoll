services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: littleboatpoll
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '8000:8000'
    volumes:
      - ./data:/app/data
